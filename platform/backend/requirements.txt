# 연합컴퓨팅 플랫폼 — 런타임 의존성
#
# 설치:
#   pip install -r requirements.txt
#
# 테스트/린트 도구는 requirements-dev.txt 참조

# FastAPI 스택
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0

# Pydantic v2 (스키마 검증)
pydantic>=2.5.0,<3.0.0

# Docker SDK (호스트 Docker 데몬과 통신 — 패키징/배포)
docker>=7.0.0

# 템플릿 (Dockerfile / Helm Chart / 대시보드 UI 렌더)
Jinja2>=3.1.0

# YAML 영속화 (15종 도메인)
PyYAML>=6.0
