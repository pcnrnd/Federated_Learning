# 서버 설정 파일
# 각 서버의 Docker 연결 정보를 정의합니다

main:
  base_url: "unix://var/run/docker.sock"
  label: "중앙 서버"
  type: "local"
  role: "central"

# 원격 서버 예시
# node1:
#   base_url: "tcp://10.0.0.5:2376"
#   label: "병원 A 서버"
#   type: "remote"
#   tls: false

